# NOLI — Harbor k8s-pull
# Vault: secret/npd-shop/harbor-pull → K8s secret harbor-pull-creds (ns npd-shop)
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: harbor-pull-creds
  labels:
    app.kubernetes.io/part-of: npd-shop
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: harbor-pull-creds
    creationPolicy: Owner
    template:
      type: kubernetes.io/dockerconfigjson
      engineVersion: v2
      data:
        .dockerconfigjson: '{{ printf "{\"auths\":{\"%s\":{\"username\":\"%s\",\"password\":\"%s\",\"auth\":\"%s\"}}}" .registry .username .password (printf "%s:%s" .username .password | b64enc) }}'
  data:
    - secretKey: registry
      remoteRef:
        key: npd-shop/harbor-pull
        property: registry
    - secretKey: username
      remoteRef:
        key: npd-shop/harbor-pull
        property: username
    - secretKey: password
      remoteRef:
        key: npd-shop/harbor-pull
        property: password
